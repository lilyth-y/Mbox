Tier 2 qualitative review notes:

- Label plausibility: compare returned labels against visible subject in each fixed sample.
- Center plausibility: verify crop focus stays near the declared center percentages.
- Focus on subject: confirm `focus.onPrimarySubject` matches the visible main subject.
- Aesthetic framing: compare `focus.aestheticScore` and `focus.compositionNotes` with the intended crop.
- Crop framing: confirm 1024x1024 output preserves the main subject without clipping.
- Latency: record analyze and edit durations separately for each sample.

Store exported review images in this directory after manual or scripted review runs.
